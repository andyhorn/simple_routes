name: simple_routes
description: Simple, type-safe route and navigation management for go_router.
version: 0.0.12
homepage: https://github.com/andyhorn/simple_routes

environment:
  sdk: ">=3.1.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  go_router: ^12.1.1
  mocktail: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
