name: simple_routes
description: Simple, declarative route and navigation management for go_router.
version: 2.0.0-rc.2
homepage: https://github.com/andyhorn/simple_routes

environment:
  sdk: ">=3.1.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  go_router: ">=12.0.0 <15.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ">=2.0.0 <4.0.0"
  mocktail: ^1.0.1

topics:
  - go-router
  - navigation
  - routing
